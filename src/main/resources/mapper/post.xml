<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.banjjak.post.model.mapper.PostMapper">

    <resultMap type="postDto" id="post" >
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="region" property="region"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="recruits" property="recruits"/>
        <result column="date_time" property="dateTime"/>
        <result column="place" property="place"/>
        <result column="create_date" property="createDate"/>
    </resultMap>

    <update id="update" parameterType="postDto">
        UPDATE post
        SET user_id = #{userId},
            region = #{region},
            title = #{title},
            content = #{content},
            recruits = #{recruits},
            date_time = #{dateTime},
            place = #{place},
            create_time = #{createTime}
        WHERE post_id = #{postId}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM post
        WHERE post_id = #{postId}
    </delete>

    <!-- CRUD-->

    <insert id="writePost" parameterType="postDto" useGeneratedKeys="true" keyProperty="postId" keyColumn="post_id">
        INSERT INTO post (user_id, region, title, content, recruits, date_time, place)
        VALUES (#{userId}, #{region}, #{title}, #{content}, #{recruits}, #{dateTime}, #{place})
    </insert>

    <select id="listPost" resultType="list" resultMap="post">
        SELECT *
        FROM post
    </select>

    <select id="detailPost" parameterType="long" resultMap="post">
        SELECT *
        FROM post
        WHERE post_id = #{postId}
    </select>

</mapper>